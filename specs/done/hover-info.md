# Unified Hover Info

All views show enriched hover with passenger count, mode share %, and recovery % (when post-2019 data exists). Uses Plotly `customdata` + `hovertemplate`. Trace name and year shown in the `<extra>` tag.
