# Crossing/Mode Icons

SVG data-URI icons in scatter view, positioned after each trace's last-year bubble via Plotly `layout.images`. Branded logos (PATH, NJT, Port Authority, NYWW ferry, Amtrak) in the legend grid.

## Status

- Scatter view: done (icons in chart + legend)
- Bar/pct/recovery views: TODO (icons in legend only)
- Logo files: `www/public/*.svg` and `www/public/img/`
